"use strict";

function makeInitState() {
	let state = {}
	console.warn("Implement the starting state of the game here")
	return state
}

function tick(time, events, state, send) {
	// apply events, then tick update state to the next state
	console.warn("No game logic implementation yet")
}
